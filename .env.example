# Copy this file to .env and fill in your key to enable the AI Care Assistant's
# Claude-backed task suggestions. Without a key, the assistant automatically
# falls back to a deterministic offline draft - the app still runs fully.
ANTHROPIC_API_KEY=sk-ant-...
